# 任务

- [x] 更新四个高频 `SKILL.md`，加入简短 UI 设计路由规则，保持热路径合计不超过 7600 字符。
- [x] 新增 `aiopc-ui-design/REFERENCE.md`，按章节提供渐进式披露和快速路由。
- [x] 更新 `aiopc-propose/REFERENCE.md`，引用共享 UI 设计规范并说明不启用/light/full 推荐规则。
- [x] 更新 `aiopc-apply/REFERENCE.md`，说明存在 `ui-design.md` 时的实现联动、Action Contract、State Ownership 和 UI 验证要求。
- [x] 更新 `aiopc-accept/REFERENCE.md`，说明存在 `ui-design.md` 时的 UI 验收、证据和追踪矩阵要求。
- [x] 更新 `aiopc-loop/REFERENCE.md`，说明只传递 UI 设计上下文，不全量解析。
- [x] 增强或新增静态校验脚本，验证热路径大小、按需读取规则、共享 reference 结构、关键模板和联动规则。
- [x] 更新变更前后对比记录和 implementation change-log。
